# Dependencias de desarrollo y pruebas (NO requeridas en produccion).
# Instalar con: pip install -r requirements-dev.txt

-r requirements.txt

# Framework de pruebas
pytest==8.2.0
pytest-cov==5.0.0
pytest-mock==3.14.0

# Cliente HTTP para pruebas E2E con TestClient de FastAPI
httpx==0.27.0

# Control determinista de fecha/hora en pruebas
freezegun==1.5.1
